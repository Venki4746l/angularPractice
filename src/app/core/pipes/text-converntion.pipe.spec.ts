import { TextConverntionPipe } from './text-converntion.pipe';

describe('TextConverntionPipe', () => {
  it('create an instance', () => {
    const pipe = new TextConverntionPipe();
    expect(pipe).toBeTruthy();
  });
});
